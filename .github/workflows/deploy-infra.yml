name: deploy-infra
# Reusable workflow (workflow_call) — the callee end of the cross-repo pipeline edge.
on:
  workflow_call:
permissions:
  contents: read
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - run: echo "reusable infra deploy"
