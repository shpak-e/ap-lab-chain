name: downstream
on:
  push:
  workflow_run:
    workflows: [ci]
    types: [completed]
permissions:
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "downstream of ci"
# ActionsPlane live-validation Stage A trigger 2026-07-24T09:12Z
